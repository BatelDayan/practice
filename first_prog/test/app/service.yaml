apiVersion: v1
kind: Service
metadata:
  name: batel-app-service
  namespace: batel-ns
spec:
  type: NodePort
  selector:
    app: batel-app
  ports:
    - protocol: TCP
      port: 3000       # In
      targetPort: 3000
      nodePort: 31000  #out
