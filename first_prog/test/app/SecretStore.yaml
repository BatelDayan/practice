apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: batel-aws-secret-store
  namespace: batel-ns
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-2
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
