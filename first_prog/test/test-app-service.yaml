apiVersion: v1
kind: Service
metadata:
  name: batel-test-service
  namespace: batel-ns
  labels:
    app: batel-test-app
spec:
  type: NodePort
  selector:
    app: batel-test-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
